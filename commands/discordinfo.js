const Discord = require("discord.js");
const { formatDate } = require("./utils/playerinfo.utils");

exports.run = (bot, message) => {
    // Créer une commande qui envoie un embed avec des informations sur le discord.
    // L'embed doit contenir les informations suivantes :
    // - Le nom du discord
    // - La date de création du discord
    // - Le nombre de personnes sur le discord
    // - Le fondateur du discord
}
