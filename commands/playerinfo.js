const Discord = require("discord.js");
const { getMember, formatDate } = require("./utils/playerinfo.utils");

exports.run = (bot, message, args) => {
    // Créer une commande qui envoie un embed avec des informations sur la personne mentionnée.
    // La commande aura cette structure : !playerinfo @user
    // L'embed doit contenir les informations suivantes :
    // - Le nom de la personne mentionnée
    // - Le tag de la personne mentionnée
    // - L'ID de la personne mentionnée
    // - La date de création du compte discord de la personne mentionnée
    // - La date d'arrivée sur le discord de la personne mentionnée
    // - Les rôles de la personne mentionnée
}
